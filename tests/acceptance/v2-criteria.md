# v2 acceptance criteria, from the developer's brief of 2026-09-16

Each row is one requirement the developer stated when commissioning v2, in their own terms. The
agent fills the "Agent check" column with evidence before handing over; the developer fills the
"Human verdict" column. `v2.0.0` is tagged only when every human verdict reads PASS or ACCEPTED.

| # | Requirement (developer's brief) | Where it lives now | Agent check (evidence) | Human verdict |
|---|---|---|---|---|
| 1 | One directory the developer maintains, holding rolling-wave-planning, pre-rolling-wave-planning, human-assisted-verification and the mentor documentation system; third-party skills (superpowers, etc.) stay out | Repo root `SKILL.md`; `skills/pre-rolling-wave-planning`, `skills/human-assisted-verification`, `skills/mentor-documentation-system`; symlinks in `~/.agents/skills` | `find . -type f` lists only the six family entries under `skills/`; no third-party skill dir ships. `./setup.sh --check` exits 0, prints `6/6 skills resolve`. `ls -la ~/.agents/skills` shows `rolling-wave-planning` as a real dir plus 5 symlinks into it. Externals are named, not vendored: `setup/referenced-skills.txt` (36 lines of name + install command). | open |
| 2 | The bloated SKILL.md is decomposed so only relevant information loads at any given time | `SKILL.md` router (57 lines) + `references/` loaded one per phase; README "What loads when" table | `wc -l SKILL.md` = 57; its phase table `SKILL.md:28-38` loads exactly one target and line 8 forbids a second. `references/` = 9 files, 41-157 lines. README "What loads when" at `README.md:230-263`. See notes [2]: three line counts in that table and at `README.md:180` are one short of `wc -l`. | open |
| 3 | It is a state/context management system: quit at 30% context in any state, even mid-rant or mid-interview, and resume | `00-plan.md` `phase:` field; `planning/` checkpoints per pre-phase; `references/resume.md`; scenarios 01 and 02 | `phase:` field: `templates/00-plan.md:10-11`; per-phase checkpoints `skills/pre-rolling-wave-planning/SKILL.md:18-29`; pause/resume ceremony `references/resume.md:66-78`; `SKILL.md:37,42`. Scenario 01 re-run PASS (`runs/01b.md`: read 00-plan, resume.md, item 4 card + feature + working file only). Scenario 02 re-run in flight today (`runs/fx02c`, `fx02d`, `fx02e`); orchestrator-graded 2026-09-16 result still pending. Scenario 02 re-run 2026-09-16: runs one and two exposed a prompt gap and a resume-rule defect (both fixed, README § Tests), run three PASS on all five criteria (orchestrator-graded). | open |
| 4 | The start of a rolling wave is the developer ranting; from there, solutions and design decisions | `pre-rolling-wave-planning` Phase 0 intake; `templates/intake.md` keeps the rant verbatim | `skills/pre-rolling-wave-planning/SKILL.md:31-43` Phase 0: the developer rants, and the SSOT dir + `00-plan.md` + `planning/00-intake.md` are written before anything else. `templates/intake.md:5-9` heads the file `## Rant (verbatim)` with "pasted unedited... Do not summarise, reorder or correct it here". | open |
| 5 | UI first: a rough wireframe of screens and their features (basic HTML), not implementation, so backend needs surface and agents do not invent unintuitive UIs | Phase 3 blueprint; `references/blueprint.md`; `templates/wireframe.html`; `ui-guidelines` role | `skills/pre-rolling-wave-planning/SKILL.md:69-73` (Phase 3, skip note required); `references/blueprint.md:7,13-19` inventory-not-design, plain HTML, controls/states/nav/roles; `templates/wireframe.html:1-18` is boxes-and-labels with a 6-rule style block. Backend needs surface at `blueprint.md:24-30`. `ui-guidelines` role is blueprint-only lookup: `references/dispatch.md:56`, `blueprint.md:18`. | open |
| 6 | Pre-rolling-wave focuses on clarifying, interviewing and grilling for long-term decisions; methodology moves out of it | Pre-planning Phase 4 interview via `grilling`; execution discipline removed from pre-planning, fixed defaults in `references/lifecycle.md`, `review.md`, `verification.md` | `skills/pre-rolling-wave-planning/SKILL.md:75-89` runs `grilling`, framing-challenge, decisions table, adapter final round. `:106-108` "Execution discipline is not a phase", pointing at `lifecycle.md`/`review.md`/`verification.md`. `git show v1.0.0:SKILL.md` line 130 held `## Execution discipline`, so it did move out. | open |
| 7 | Rolling-wave mentions and enforces TDD | `references/lifecycle.md` `in-progress` gate names `superpowers:test-driven-development`; `tdd` role on every implementer packet | `references/lifecycle.md:111-113` TDD is the fixed default via `superpowers:test-driven-development`, exception = a dated card line; gate `lifecycle.md:46`. `references/dispatch.md:54` binds the `tdd` role. Wording check: that row reads "Every packet that writes production code", not literally every implementer packet. | open |
| 8 | Retain all the information in both skills; consolidate, do not lose | `CHANGELOG.md` 2.0.0 lists every drop; retention audit in the plan file `/Users/joshi/.claude-account2/plans/i-wanna-work-on-encapsulated-biscuit.md` § Retention audit; `git show v1.0.0:` for the originals | `CHANGELOG.md` § Removed lists the drops; plan file § Retention audit maps every v1 heading to a v2 file or the drops list (reviewer searched the wrong directories; path is in the location column); `git tag` shows `v1.0.0`. | open |
| 9 | Push back and suggest better approaches; prefer existing proven tools | Brainstorm record in the plan file `/Users/joshi/.claude-account2/plans/i-wanna-work-on-encapsulated-biscuit.md` (decisions table, 14 rows); kernel-plus-drivers frame; existing skills bound by role rather than re-implemented | Plan file § Decisions settled in the brainstorm holds 14 decision rows with the alternatives weighed; `references/dispatch.md:16` names skills and forbids paraphrase; `README.md` § Kernel plus drivers states the frame. | open |
| 10 | A central orchestrator with fresh-context subagents that use existing skills (ponytail, agent-browser, ui-ux-pro-max, next/stripe/supabase skills); a step that finds the right skill for a task, dynamically | `references/dispatch.md` packet contract, role table, description-grep fallback; `02-adapters.md` bindings | `references/dispatch.md:9-19` seven required slots, `:21-29` tier-by-role table, `:31-35` separation of agents, `:47-73` role tables naming ponytail / agent-browser / ui-ux-pro-max / next / stripe / supabase skills, `:75-86` description-grep fallback. Bindings live in `templates/02-adapters.md:83-107`. Scenario 03 re-run PASS, orchestrator-graded 2026-09-16: notes [10]. | open |
| 11 | Better verification of web and mobile: agent-browser, mobile simulator/MCP; levels: sub-feature, features together, human | `references/verification.md` ladder L1 to L5; L4 cross-feature at item level; surface coverage gate in `references/adapters.md` | `references/verification.md:13-19` L1-L5 table, `:36-51` L3 web via `agent-browser` / mobile via the bound tool / uncovered-surface rule, `:53-59` L4 at item level, gated in `references/lifecycle.md:85`. Surface coverage gate `references/adapters.md:116-135`; mobile candidate family `:43-51` (mobile-mcp, Maestro, ios-simulator-mcp, Appium, Detox). | open |
| 12 | Raise confidence with agent-driven testing first; human-assisted steps only for the rest; no agent steps inside human verification (the human runs the SQL/CLI); two scores, agent-only and after-human | `skills/human-assisted-verification/SKILL.md` (zero agent steps, verdicts `open`); `agent` and `ceiling` in `references/verification.md` and `templates/feature.md` | `skills/human-assisted-verification/SKILL.md:14-24` (L1-L4 finished first, zero agent steps), `:47` the "Check it yourself" column, `:49` verdicts human-only and the agent hands over `open`, `:91-93` interrupt handshake removed. Two scores: `references/verification.md:106-111`, `templates/feature.md:52`. Scenario 04 re-run PASS (`runs/04c.md`: every verdict `open`, exclusions listed). | open |
| 13 | Confidence scores stay current across later phases, or say they will rise | Evidence log + re-derivation rule in `references/verification.md`; resume sweep promotes on human ticks | `references/verification.md:118-120` re-derivation rule (append evidence row, re-derive both numbers with a fresh date, stale date = drift) plus the evidence log `:68-83`; `references/resume.md:45` the sweep re-derives and dates on promotion; `references/lifecycle.md:104` re-derives at `documented -> complete`. | open |
| 14 | Code review more often; a reuse review (the Supabase generated-types example) | `references/review.md` four review points; reuse and duplication dimension | `references/review.md:14-21` four review points (branch, feature PR, item PR, batch PR) and where each posts; `:56-76` dimensions, with dimension 2 the reuse ladder and `:66-69` the generated-database-types example verbatim ("a hand-written type that duplicates a generated database types file is a finding"). | open |
| 15 | Human verification central and feature-specific; testing per feature and for features together | `01-verification.md` index + `verification/<n>.<f>-<slug>.md` + `group-<slug>.md`; L4 rows | `references/ssot-layout.md:44` `01-verification.md` is the index only; rows live in `verification/<n>.<f>-<slug>.md` and `group-<slug>.md` (`references/verification.md:61-66`, `ssot-layout.md:27`). Index-row and group shapes: `templates/verification-feature.md:38-56`. Features-together testing is L4 at the item gate, `references/lifecycle.md:85`. | open |
| 16 | Documentation: mentor-documentation-system moved under; human docs written by an external CLI (codex today) from a feature boundary plus terms; decoupled from any model | `templates/doc-handoff.md`; docs-writer binding in `02-adapters.md` with subagent fallback; `human-engineering-docs` docs-writer contract | `skills/mentor-documentation-system/` ships in-repo (26 files) with `human-engineering-docs` and `senior-mentor` nested. `templates/doc-handoff.md:5-10` sends the filled prompt to a CLI or a `heavy` subagent and `:10` states the writer sees only what is below the line; `:18-37` conventions path, boundary file list, terms. Binding row `templates/02-adapters.md:51-56`; contract `human-engineering-docs/SKILL.md:57-63`. | open |
| 17 | Model-agnostic: no explicit model labels in the skill | tiers `judge`/`heavy`/`light`; vendor names only as examples in `references/adapters.md` (grep proves it) | `grep -rniE '\b(opus\|sonnet\|haiku\|codex\|gemini\|gpt\|claude)\b' SKILL.md references skills/*/SKILL.md templates` returns exactly 3 hits, all allowed. Judgement per hit in notes [17]. | open |
| 18 | Out-of-scope work gets its own SSOT (a sibling dir with a README, brief description); improvements to the current batch stay in the flow | `references/resume.md` mid-flight inputs; `templates/deferred-README.md` | `references/resume.md:47-53` triages every mid-flight input into in-scope (ledger row + card at its execution slot), out-of-scope (sibling stub dir, never a file inside the batch), or `triage`; `:51` makes an improvement to a feature this batch built in-scope by definition. Stub shape: `templates/deferred-README.md` (29 lines: description, why out of scope, cold-pickup context, origin). | open |
| 19 | Git init, public repo, first commit of the initial version as a revert point | github.com/dirghayu101/rolling-wave-planning; tag `v1.0.0` | `git remote -v` = `git@github.com:dirghayu101/rolling-wave-planning.git`; `gh repo view` returns `"visibility":"PUBLIC"`, default branch `main`. `git tag` lists `v1.0.0`; `git show v1.0.0 --stat` = commit b42a274 "import ... verbatim", 30 files, 1262 insertions, 0 deletions, message "No content edits. Tag v1.0.0 is the revert point." | open |
| 20 | Version the skill | `VERSION`, `CHANGELOG.md`, git tags | `cat VERSION` = `2.0.0`; `CHANGELOG.md` has dated 2.0.0 and 1.0.0 entries under "Versions follow semver"; `SKILL.md:55-57` and `README.md:425-443` point at both. `git tag` returns only `v1.0.0`: **v2.0.0 does not exist yet**, which is expected, since the tag waits on the human verdicts in this very file. | open |
| 21 | Line limits are soft and apply to documentation files, not agent files | `references/ssot-layout.md`; `validate_docset.py` warns | `references/ssot-layout.md:62-66`: soft ~100-line target on the four human-facing file kinds only, agent files explicitly uncapped but pruned. `validate_docset.py:12-14` comments that the cap "is now a warning, because v2 line limits are soft targets", `SOFT_MAX_LINES = 100`; `:47,83-90` push it into `warnings` and still print `PASS: 0 errors, N warning(s)`. | open |
| 22 | Lifecycle: pending, in-progress (TDD), code-reviewed, agent verification, agent verified, documented, human verification, completed; the agent is done at documented | `references/lifecycle.md` feature and item stage sets (`reviewed`, `agent-verified`, `documented`, `merged` / `complete`) | `references/lifecycle.md:19-21` feature `pending -> in-progress -> reviewed -> agent-verified -> documented -> merged`, item `pending -> in-progress -> agent-verified -> documented -> complete`; `:23` triage/blocked/deferred; `:25` `documented` is the agent terminal at both scopes; `:101-107` only the human or the tick-reading sweep sets `complete`. Brief names map: code-reviewed = `reviewed`, agent verification = the `reviewed -> agent-verified` gate. | open |
| 23 | Keep all the good parts; the skill already works | Retention audit; execution-slot rules, ceremony, pause protocol, integrity sweep all carried over with their evidence sentences | Each v1 evidence sentence survives verbatim in its v2 home: v1 `SKILL.md:28` -> `ssot-layout.md:11` (two dirs numbered 9); v1:48 -> `ssot-layout.md:37` (969-line plan); v1:56 -> `ssot-layout.md:44` and `verification.md:137-138` (2 of 4 batches); v1:71 -> `resume.md:64` (batch 10, 2026-09-04); v1:112 -> `lifecycle.md:99` (8/9 then 0/8 working files). | open |
| 24 | Verification tooling as a kickoff step (mobile MCP next time); suggest open-source tools | Surface coverage check and candidate table in `references/adapters.md`; mashric `adapters.default.md` records the mobile gap and the Maestro finding | `references/adapters.md:24-100` gives one candidate table per family (browser, mobile, backend, docs writer, load, visual diff, code map), each row carrying a detection command and an install command, with `:26` warning to verify installs against the tool's own README. Kickoff placement: `:3`, `skills/pre-rolling-wave-planning/SKILL.md:85-89`. mashric `adapters.default.md:27-32` records the mobile gap and the Maestro finding. | open |
| 25 | Skills like ponytail are referenced fresh so upgrades flow through | `references/dispatch.md`: name skills, never paraphrase; subagent invokes them itself | `references/dispatch.md:16`: resolve the role, write the pair on the packet, "The subagent invokes each named skill itself"; "Never paraphrase a skill's content into a packet: paraphrase goes stale the moment the skill is updated". Same rule as a red flag at `:91`. `references/adapters.md:5` and `README.md:190-194` make it repo policy. | open |
| 26 | The adapter config lives in the SSOT, lists available tools by category with instructions, a default is generated during pre-planning, and the developer swaps values | `templates/02-adapters.md`, `templates/adapters.default.md` (project defaults at `<project root>/adapters.default.md`), `references/adapters.md` detection and delta procedure | `templates/02-adapters.md` (109 lines): tiers, one section per surface, docs writer, load testing, visual diff, code map, skill roles, each with Chosen / Alternatives detected / Invocation / `enabled:`. `templates/adapters.default.md:5-9` puts the defaults at `<project root>/adapters.default.md`. Detection + delta procedure `references/adapters.md:9-22,106-114`. Generated instance: mashric `adapters.default.md`. | open |
| 27 | graphify set up in the mashric repo, with an enable/disable switch | mashric branch `chore/graphify-code-map`: `.graphifyignore`, gitignored `graphify-out/`, repo-root `adapters.default.md` code-map row `enabled: true` | mashric on `chore/graphify-code-map` (`git branch --show-current`), top commits 9f5d28d1 / e0632eab / 7b2be3fd. `.graphifyignore` present (secrets, deps, build, scratch). `git check-ignore -v graphify-out/graph.json` -> `.gitignore:48:graphify-out/`. Switch: `adapters.default.md:58` `enabled: true` and `:62` "Flipping `enabled` to `false` is the whole off switch". `graphify hook status`: all three not installed. | open |
| 28 | README: every referenced skill, setup for macOS and Windows, skills.sh pointer, the problem explained (many skills, models, tools; configure the best without burning context), integrates the best of everything, written with a writing skill, no em dashes; verified to solve the stated problem | `README.md`; scenario 05 problem fit | `grep -c '—' README.md` = 0. 447 lines. skills.sh pointer `:14-38`, macOS/Linux + Windows setup `:40-73`, problem `:149-174`, integrates-the-best `:176-194`. Scenario 05 PASS (`runs/05.md`: three forces, five mechanisms, phase table). GAP: `efficient-fable` is named in `references/dispatch.md:7` and `setup/referenced-skills.txt:39` but appears in no README table. Also notes [28]. After review: `efficient-fable` added to the README referenced-skills table (it is named at `references/dispatch.md:7`), so every skill in `dispatch.md` is now listed. | open |
| 29 | Per-role skill overrides in the config (e.g. swap the UI/UX skill used to build on top of the wireframe) | Skill roles section of `02-adapters.md`; `ui-implementation` role | `templates/02-adapters.md:83-100` is the per-role override table (Role / Chosen skill / Alternatives detected / When applied), with `ui-implementation` at `:93`; `:109` appends roles found mid-batch. Worked swap example in `README.md:304-308`. Live instance: mashric `adapters.default.md:74` binds `ui-implementation` to `frontend-design:frontend-design` with five detected alternatives. | open |
| 30 | The test runs are documented in the README for reference | README § Tests (`README.md`, the Tests heading and its scenario table) | README § Tests documents the five scenarios, what each failure changed, and the installer suite; scenario 02 and 03 rows updated 2026-09-16 with the re-runs after the path changes. Line counts in § What loads when re-derived from `wc -l`. | open |
| 31 | The confidence rubric carries actionable steps ("what would raise this"), and agent-actionable raises are done, not listed | `references/verification.md`; `templates/feature.md` "What would raise this" | `references/verification.md:122-126`: "What would raise this" is the payload, "Exhaust agent-actionable raises first: anything an agent can execute ... is *done*, not listed", and an agent-actionable entry still listed at feature close is a red flag (repeated `:157`). Template slot `templates/feature.md:57-60`; enforced at the gate `references/lifecycle.md:57`. | open |
| 32 | Cheap agents do the dumb, simple verification work (take screenshots, compare against the agreed wireframe) so the orchestrator does not | `light` tier in `references/dispatch.md`; L3 comparison in `references/blueprint.md`; visual-diff adapter | `references/dispatch.md:27` puts screenshot capture, scripted flows and checklist grading on the `light` tier, `:29` tier follows role not size. `references/blueprint.md:35`: "a light-tier agent compares the screenshot against the wireframe control by control and state by state", with the visual-diff adapter producing the diff. Adapter family `references/adapters.md:77-85`. | open |
| 33 | Some features must be tested together; the SSOT has a place for that testing phase | L4 cross-feature pass at item `agent-verified`; `verification/group-<slug>.md` for human group checks | `references/verification.md:53-59` defines L4 as the item's features exercised together (and named cross-item groups), gating `agent-verified`; enforced at `references/lifecycle.md:85`. The SSOT place is `verification/group-<slug>.md` (`ssot-layout.md:27`, `verification.md:64-66`), whose shape and write-timing are `templates/verification-feature.md:45-56`. | open |
| 34 | Documentation is not coupled to codex or to this skill's model; tomorrow's writer can be swapped | docs-writer binding in `02-adapters.md`; `templates/doc-handoff.md` is self-contained for any CLI | `templates/02-adapters.md:53-56`: Chosen is `<docs-writer-command> \| subagent`, and "`subagent` is a normal value, not a fallback failure". `templates/doc-handoff.md:5-10` works either way and states the writer "has no access to this conversation"; `:16-52` is fully self-contained (conventions path, card, feature file, boundary list, terms, output contract, stop conditions). Also `references/adapters.md:60-64`. | open |
| 35 | Recording the model per dispatch was model-dependent; the record must be model-agnostic | Dispatch record holds tier and roles, never a model name; rogue-check reads tiers against `02-adapters.md` | `references/dispatch.md:39-45`: the dispatch record columns are `Date \| Packet \| Tier \| Roles -> skills \| Outcome`, no model column. `references/verification.md:83`: "`by` is the role and tier that produced it, never a model name". Rogue-check reads tiers against `02-adapters.md`: `references/adapters.md:137-139`, `references/review.md:90-100`. Red flag for a vendor name in a packet: `adapters.md:149`. | open |
| 36 | Human verification steps must not break the developer's flow: no "make the edit, then ask the agent to check via MCP" | Each L5 row's "Check it yourself" column holds the exact SQL/CLI/console path; no interrupt handshake | `templates/verification-feature.md:26-28`: the "Check it yourself" column holds the exact thing pasted ready to use, with a full SQL example; `skills/human-assisted-verification/SKILL.md:47` adds "a row that says 'check the tickets table' is unfinished". No handshake: `SKILL.md:91-93` "There are no agent steps left to interrupt, so the handshake is gone". | open |
| 37 | Ceremony (branches, PR ladder, issues), pausing, execution-slot numbering and the integrity sweep are kept intact | `references/ceremony.md`, `references/resume.md` | `references/ceremony.md` (152 lines): branch naming `:7`, PR ladder `:41`, issues and the GitHub tree `:57`, platform linking `:77`, renumbering and pausing `:113`, levels ON/lighter/OFF `:129-137`. `references/resume.md:55-64` execution-slot numbering with the batch-10 evidence, `:66-78` the pause and resume ceremonies, `:30-45` the integrity sweep. | open |
| 38 | Explain in the README how to configure the best skills, models and tools without burning context, and how new tools such as graphify assimilate | README "Kernel plus drivers", "What loads when", "Adapters and roles", "The code map as an example driver" | `README.md:176-194` Kernel plus drivers, `:230-263` What loads when (per-phase file + line counts + the context-budget argument), `:266-347` Adapters and roles (two layers, delta-only interview, surface coverage, the `enabled:` toggle). Column name is slightly off: the graphify section is headed "The code map as an example driver" (`:325`), not "Optional: a code map". | open |

Added 2026-09-16 (post-compaction review round), from the developer's follow-up:

| # | Requirement (developer's brief) | Where it lives now | Agent check (evidence) | Human verdict |
|---|---|---|---|---|
| 39 | README leads with setup and use; people care about using it before understanding it | `README.md` Quick start and Referenced skills before The problem | `grep -n '^## ' README.md`: Quick start 12, Referenced skills 91, The problem 149, Kernel plus drivers 176, How a batch flows 196, What loads when 230, Adapters and roles 266, Verification and confidence 349, Tests 375, Versioning 425, License 445. Install and use precede The problem. `README.md:12-89` is install + first use. | open |
| 40 | `adapters.default.md` is created at the project root by default, since projects have several feature dirs, spec dirs and monorepos | `references/adapters.md`, `references/ssot-layout.md`, `templates/adapters.default.md`, `skills/pre-rolling-wave-planning/SKILL.md`; mashric `adapters.default.md` at the repo root | `references/adapters.md:11` and `templates/adapters.default.md:5` both put it at the git root "not inside any feature or spec directory", with the several-feature-dirs/monorepo reason stated; `references/ssot-layout.md:46`; `skills/pre-rolling-wave-planning/SKILL.md:88`. In mashric, `ls -la adapters.default.md` at the repo root, moved there by commit 9f5d28d1 "move to repo root, trim to chosen values". | open |
| 41 | The generated defaults file supplies chosen values, never asks the developer to choose; comments only where they raise output quality (tool absence with detect command, invocation notes, honest gaps); machine paths only when an agent needs them; no em dashes | mashric `adapters.default.md` (92 lines); Capacitor gap closed as a row rather than narrated | mashric `adapters.default.md` is **92** lines, not 92 (`wc -l`). `grep -c '—'` = 0. Every section states a Chosen value; no question is put to the developer. Comments are detect commands (`:25,31,49,54`), invocation notes (`:15,42`) and honest gaps (`:29-32,90`). One machine path, `:30 /opt/homebrew/bin/maestro`, where the agent must shell out. Capacitor is a row, `:90`. | open |
| 42 | graphify installed at `~/.agents/skills` and linked everywhere else, following the existing convention | `~/.agents/skills/graphify` real dir; `~/.claude/skills/graphify` relative symlink; `references/dispatch.md` code-map row | `ls -la ~/.agents/skills/graphify` is a real directory (drwxr-xr-x, 2026-09-16 15:27), same shape as the other family entry. `readlink ~/.claude/skills/graphify` = `../../.agents/skills/graphify`, a relative symlink. `references/dispatch.md:63` carries the `code-map` role bound to `graphify` with the install-once-globally note. Notes [42] on the convention. | open |
| 43 | A bash script and a PowerShell script set up the symlinks and everything needed, tested in a container; the README says what the script does | `setup.sh`, `setup.ps1`, `setup/referenced-skills.txt`, `tests/setup/run.sh`; README Quick start | `setup.sh` 214 lines, `setup.ps1` 246, `setup/referenced-skills.txt` 36 entries. `./setup.sh --check` exits 0 and prints `6/6 skills resolve` plus per-skill present/missing and PATH tools. `bash tests/setup/run.sh` exits 0; last three lines: `bash cases exit: 0    powershell cases exit: 0` / `wall time: 21s` / `ALL CASES PASS` (26 bash + 18 pwsh). README `:40-73` describes it. | open |
| 44 | Docker installed on this machine (CLI for the tests; Desktop GUI requested as well) | colima + docker CLI running; Docker Desktop cask needs the developer's sudo password | `command -v docker colima` -> `/opt/homebrew/bin/docker`, `/opt/homebrew/bin/colima`; `docker info` succeeds (`up`); `colima status` reports running on macOS Virtualization.Framework, aarch64, docker runtime. The CLI half is satisfied and the container suite ran on it. GAP: `ls /Applications \| grep -i docker` returns nothing (exit 1), so Docker Desktop GUI is absent; its Homebrew cask needs the developer's sudo password. | open |
| 45 | Sub-skills work when installed as siblings by skills.sh, not only inside the repo | path rule in `skills/*/SKILL.md` resolves against the `rolling-wave-planning` skill dir | `grep -n 'skill directory' skills/*/SKILL.md skills/mentor-documentation-system/skills/*/SKILL.md` -> 3 hits: `pre-rolling-wave-planning/SKILL.md:14`, `human-assisted-verification/SKILL.md:17`, `human-engineering-docs/SKILL.md:59`. Each says paths resolve against the `rolling-wave-planning` skill directory in the same skills folder, repo root when read in-repo. `senior-mentor/SKILL.md` needs no rule: its only paths are its own `references/*` (`:21-31`). | open |


## Evidence notes

Keyed by row number. Written 2026-09-16 by the verification pass; no other column was touched.

**[2]** `README.md:180` says "the largest reference file is 156" and its two tables give `references/verification.md` 156, `references/dispatch.md` 98, `references/lifecycle.md` 132 and `human-assisted-verification` 96. `wc -l` gives 157, 99, 133 and 98 (all four files end in a newline, so `wc -l` is the true line count). Off by one in four places; every other figure in those tables matches.

**[8]** Every heading of `git show v1.0.0:SKILL.md` has a v2 home: Overview -> `SKILL.md:10-18`; Unit hierarchy -> `lifecycle.md:7-15`; Step 0 The SSOT directory -> `ssot-layout.md:9-13`; Item numbers are execution slots -> `resume.md:55-64`; Three-tier file naming -> `ssot-layout.md:68-76`; Confidence score rubric not formula -> `verification.md:91-129`; The loop -> `lifecycle.md:29-35`; Lifecycle two scopes -> `lifecycle.md:17-27`; Pausing a batch -> `resume.md:66-78`; Git and GitHub ceremony -> `references/ceremony.md`; Execution discipline -> `lifecycle.md:111-113` plus `review.md` and `verification.md`; Mid-flight inputs -> `resume.md:47-53`; Resumability protocol -> `resume.md:5-10`; Red flags -> one section per reference file; Delegation, this skill is a conductor -> `dispatch.md:5-7`. `git ls-tree -r --name-only v1.0.0` shows v1 held only `SKILL.md`, `ceremony.md`, `feature-file-template.md` and the three sub-skills, so that list is the whole retention surface.

**[10]** Scenario 03 was re-run today against the criteria corrected in HEAD 3d8a4b5 and graded by the orchestrator (orchestrator-graded 2026-09-16), PASS: an implementer packet for feature 4.3 at tier `heavy`, roles resolved to `ponytail`, `superpowers:test-driven-development`, `frontend-design:frontend-design` and `next-best-practices`, no vendor names anywhere, exactly three SSOT paths, and L3 explicitly deferred to a separate agent. The transcript was not saved to a file, so the grading itself is the citation; the earlier saved run `runs/03b.md` is the previous criteria version (the 4.2 L3 packet).

**[17]** The three hits, with a judgement each: `references/adapters.md:11` "the project's `CLAUDE.md` or `AGENTS.md`" - allowed, a filename, not a model label. `references/adapters.md:30` "Examples of the alias strings a harness might expose: `opus`, `sonnet`, `haiku`, and non-Anthropic driver CLIs such as `codex` or `gemini`" - allowed, this is the sanctioned examples-only location. `templates/adapters.default.md:9` "the project's `CLAUDE.md` or `AGENTS.md`" - allowed, a filename in a template placeholder block. No hit in `SKILL.md`, `skills/*/SKILL.md`, or any other reference or template.

**[28]** Two further observations on the README, neither an em dash issue. (a) `efficient-fable` is a skill the kernel names (`references/dispatch.md:7` sends the reader to it for packet craft) and `setup/referenced-skills.txt:39` ships its install command, but it is in none of the three README tables, although `README.md:91-93` promises "Every skill named anywhere in the kernel". All 31 other skills named in `dispatch.md` do appear. (b) `tests/scenarios/05-problem-fit.md:31` grades against a README section called "Who this is for"; no such heading exists (`grep -n '^## ' README.md`) and the content sits in the opening paragraph at `README.md:3-7`, which is what the passing run `runs/05.md` answered from.

**[30]** The README Tests table at `README.md:411-421` is stale in the scenario 03 row, in two cells. Its "What changed" cell says the scenario "was reworded to 'advance the feature to its next stage'", but `tests/scenarios/03-dispatch-packet-shape.md:18` now reads "Your task is 'Implement feature 4.3.'" after commit 3d8a4b5. Its "Re-run" cell records "PASS: produced the L3 verification packet for feature 4.2", which is the saved run `runs/03b.md` against the pre-3d8a4b5 criteria; today's re-run under the corrected criteria produced a 4.3 implementer packet instead. The scenario 02 row also predates today's re-run, which is still in flight. Nothing else in the section is contradicted: the no-runner rationale matches `tests/README.md:20-26`, and the installer paragraph matches the suite's own output.

**[42]** graphify follows the convention and slightly exceeds it: like the family it is a real directory in `~/.agents/skills`, and it is the only one also linked into `~/.claude/skills` (21 entries, `graphify` among them). The five rolling-wave family entries are not currently linked into `~/.claude/skills`; `setup.sh --claude` is what would do that, and the container suite proves that path works (`PASS B6 six symlinks under $HOME/.claude/skills`).
