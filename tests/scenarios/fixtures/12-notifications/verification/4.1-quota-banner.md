# Verification 4.1: Quota Banner

Feature: `rollout/4-quota/1-quota-banner.md` · Indexed in `01-verification.md`
Written 2026-09-16. Handed over at stage `documented`; all rows PASS promotes the item to `complete`.

Setup: Web app logged in as test user with notifications enabled and quota set to 5 daily notifications.

| # | Human steps | Human-only observation | Check it yourself | 24h | Verdict + date |
|---|---|---|---|---|---|
| 1 | 1. Open notifications feed. 2. Observe quota banner at top of feed showing "1 of 5". 3. Receive 3 notifications. 4. Refresh page. 5. Observe banner now shows "4 of 5". | Banner text is clear, non-technical, and easy for a non-technical user to understand. The count updates match actual notification delivery. | Database query: `select count(*) as notification_count from notifications where user_id = '<test-user-id>' and created_at > now() - interval '1 day' and not archived;` should return 4 | no | PASS 2026-09-16 |
| 2 | 1. Open notifications feed. 2. Observe dismiss button on quota banner (usually an X or close icon). 3. Click dismiss button. 4. Verify banner disappears from view. 5. Wait 30 seconds. 6. Observe banner remains dismissed. | Banner cleanly removes from the feed without jarring layout shift. No console errors appear. Dismissed state is stable and does not flicker or reappear. | Open browser console (F12 → Console tab), set filter to "error", verify no new error messages appear after dismiss action. | no | PASS 2026-09-16 |
| 3 | 1. Dismiss quota banner. 2. Close browser tab entirely (not just navigate away). 3. Reopen app in a new tab. 4. Navigate to notifications feed. 5. Observe whether banner is dismissed or re-appears. | If banner is dismissed, user's preference is respected across sessions and the feature feels polished. If it reappears, that is also valid depending on product decision, but user expectation may be set by similar patterns in the app. | Database query: `select user_id, banner_dismissed, dismissed_at from user_preferences where user_id = '<test-user-id>';` should show `banner_dismissed = true` and `dismissed_at` within the last few minutes. Navigate to notifications again and check if banner element exists in the DOM: right-click → Inspect, search for "quota-banner" class. | no | PASS 2026-09-16 |
| 4 | 1. Open notifications feed when quota is at "5 of 5" (quota limit reached). 2. Observe banner styling, color, and messaging. 3. Receive a 6th notification. 4. Observe banner updates to show quota exceeded status (e.g., "6 of 5" or "Quota exceeded"). | Banner clearly indicates to the user that quota limit has been reached, using visual cues (color change, icon, or warning text) that stand out without being alarming. Message is clear about what "exceeded" means. | Manually verify the banner's CSS styling shows a warning color (e.g., red, orange, or high-contrast): right-click → Inspect → check background-color and color properties. Verify the message text uses clear language by reading the rendered text on screen. | no | PASS 2026-09-16 |
