# Verification 3.1: Daily Digest Schedule

Feature: `rollout/3-digest-scheduler/1-daily-digest.md` · Indexed in `01-verification.md`
Written 2026-09-08. Handed over at stage `documented`; all rows PASS promotes the item to `complete`.

Setup: Web app, test user with digest enabled.

| # | Human steps | Human-only observation | Check it yourself | 24h | Verdict + date |
|---|---|---|---|---|---|
| 1 | 1. Navigate to Settings → Notifications → Digest. 2. Select "Daily" and set time to 8:00 AM. 3. Click Save. 4. Check the settings page. | Settings save successfully with confirmation toast. Time picker displays "8:00 AM". | Database query: `select user_id, digest_frequency, digest_time from digest_settings where user_id = '<test-user-id>';` should show `daily` and `08:00:00` | no | PASS 2026-09-08 |
| 2 | 1. Wait until digest scheduled time (or trigger manually via admin panel). 2. Check email inbox. | A digest email arrives at the scheduled time with subject line "Your Daily Notifications Digest" | Check email service logs: `select * from email_logs where user_id = '<test-user-id>' and type = 'digest' order by sent_at desc limit 1;` should show sent at correct time | yes | PASS 2026-09-08 |
| 3 | 1. Open digest email. 2. Verify it contains notifications from the past 24 hours. | Email body lists 3+ notifications grouped by category. | Verify by opening the email and visually inspecting the content matches notifications from the past day | no | PASS 2026-09-08 |
| 4 | 1. Navigate to Settings → Digest. 2. Change frequency to "Weekly". 3. Verify digest is not sent the next day. | No digest email arrives within 24 hours. | Check email logs don't show a digest sent after frequency change (check timestamp after the setting change) | yes | PASS 2026-09-08 |
