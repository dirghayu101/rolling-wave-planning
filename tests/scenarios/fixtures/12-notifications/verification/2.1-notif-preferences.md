# Verification 2.1: Notification Preferences

Feature: `rollout/2-notif-prefs/1-notif-preferences.md` · Indexed in `01-verification.md`
Written 2026-09-04. Handed over at stage `documented`; all rows PASS promotes the item to `complete`.

Setup: Web app logged in as test user.

| # | Human steps | Human-only observation | Check it yourself | 24h | Verdict + date |
|---|---|---|---|---|---|
| 1 | 1. Navigate to Settings → Notifications. 2. Toggle "Email notifications" off. 3. Reload page. 4. Return to Notifications settings. | Toggle remains OFF after page reload | Check browser localStorage: `JSON.parse(localStorage.getItem('user_prefs')).notif_email` should be `false` | no | PASS 2026-09-04 |
| 2 | 1. Navigate to Settings → Notifications. 2. Set "Delivery time" to 9:00 AM. 3. Click Save. 4. Check admin panel user record. | Save succeeds and user sees confirmation toast | Database query: `select user_id, delivery_time from notification_preferences where user_id = '<test-user-id>';` should show `09:00:00` | no | PASS 2026-09-04 |
